import { LayoutProps } from "@/components/layout-prop";
import React from "react";

const IntroLayout = ({ children }: LayoutProps) => {
  return <div>{children}</div>;
};

export default IntroLayout;
